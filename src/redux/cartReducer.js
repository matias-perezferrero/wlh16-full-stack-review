const initialState = {
    cart: [],
    loading: false,
    error: false,
    errorMessage: ''
}

export default function cartReducer(state = initialState, action) {
    switch(action.type) {
        default: 
        return state
    }
} 